dependencies:
  pre:
    - go get github.com/constabulary/gb/...
    - gem install package_cloud
    - bundle
  override:
    - echo "All dependencies are vendored"

test:
  override:
    - gb test

deployment:
  production:
    branch: master
    commands:
      - ./script/build_deb